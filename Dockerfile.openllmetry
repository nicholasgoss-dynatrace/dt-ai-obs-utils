FROM python:3.11-slim

WORKDIR /app

COPY requirements_openllmetry.txt .
RUN pip install --no-cache-dir -r requirements_openllmetry.txt

COPY app_openllmetry.py .
COPY llm_client.py .
COPY log_setup.py .

EXPOSE 8000

CMD ["uvicorn", "app_openllmetry:app", "--host", "0.0.0.0", "--port", "8000"]
